<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Gaze" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body class="bg-gray-50">
    <nav class="bg-white shadow-sm border-b">
      <div class="container mx-auto px-4">
        <div class="flex justify-between items-center h-16">
          <div class="flex items-center">
            <%= link_to "GAZE", root_path, class: "text-xl font-bold text-gray-900" %>
            <% if user_signed_in? %>
              <div class="ml-10 flex items-baseline space-x-4">
                <%= link_to "Snippets", snippets_path, class: "text-gray-700 hover:text-gray-900 px-3 py-2 rounded-md text-sm font-medium" %>
                <%= link_to "Collections", collections_path, class: "text-gray-700 hover:text-gray-900 px-3 py-2 rounded-md text-sm font-medium" %>
                <%= link_to "Tags", tags_path, class: "text-gray-700 hover:text-gray-900 px-3 py-2 rounded-md text-sm font-medium" %>
              </div>
            <% end %>
          </div>
          
          <div class="flex items-center space-x-4">
            <% if user_signed_in? %>
              <%= link_to "New Snippet", new_snippet_path, class: "bg-blue-600 text-white px-4 py-2 rounded-md text-sm font-medium hover:bg-blue-700" %>
              <div class="relative">
                <%= link_to current_user.name || current_user.username || current_user.email, user_profile_path, class: "text-gray-700 hover:text-gray-900 px-3 py-2 rounded-md text-sm font-medium" %>
              </div>
              <%= link_to "Sign Out", destroy_user_session_path, method: :delete, data: { "turbo-method": :delete }, class: "text-gray-700 hover:text-gray-900 px-3 py-2 rounded-md text-sm font-medium" %>
            <% else %>
              <%= link_to "Browse", public_snippets_path, class: "text-gray-700 hover:text-gray-900 px-3 py-2 rounded-md text-sm font-medium" %>
              <%= link_to "Sign In", new_user_session_path, class: "text-gray-700 hover:text-gray-900 px-3 py-2 rounded-md text-sm font-medium" %>
              <%= link_to "Sign Up", new_user_registration_path, class: "bg-blue-600 text-white px-4 py-2 rounded-md text-sm font-medium hover:bg-blue-700" %>
            <% end %>
          </div>
        </div>
      </div>
    </nav>
    
    <% if notice.present? %>
      <div class="bg-green-50 border border-green-200 text-green-800 px-4 py-3" role="alert">
        <p><%= notice %></p>
      </div>
    <% end %>
    
    <% if alert.present? %>
      <div class="bg-red-50 border border-red-200 text-red-800 px-4 py-3" role="alert">
        <p><%= alert %></p>
      </div>
    <% end %>
    
    <main class="container mx-auto px-4 py-8">
      <%= yield %>
    </main>
  </body>
</html>
